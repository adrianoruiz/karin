// Este plugin não é necessário, pois o Pinia já está sendo registrado pelo módulo @pinia/nuxt
// Mantemos este arquivo apenas como referência, mas ele não deve registrar o Pinia novamente

// import { createPinia } from 'pinia'

// export default defineNuxtPlugin((nuxtApp) => {
//   const pinia = createPinia()
//   nuxtApp.vueApp.use(pinia)
// })
